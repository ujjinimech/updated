---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: postgresql
  namespace: flux-system
spec:
  chart:
    spec:
      chart: postgresql
      sourceRef:
        kind: HelmRepository
        name: bitnami    
  interval: 1m0s
  releaseName: postgresql
  values:
    extraEnvVars:
      - name: FOO
        value: BAR  
